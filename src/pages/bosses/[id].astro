---
import Layout from "../../layouts/Layout.astro";
import { getData } from "../../utils/getData";

type Data = {
  description: string;
  drops: string[];
  healthPoints: string;
  image: string;
  id: string;
  location: string;
  name: string;
};

export async function getStaticPaths() {
  const data = await getData("bosses");

  return data?.map(
    ({ description, drops, healthPoints, image, id, location, name }: Data) => {
      return {
        params: { id },
        props: {
          description,
          drops,
          healthPoints,
          image,
          location,
          name,
        },
      };
    }
  );
}
---

<Layout pageTitle={Astro.props.name} title={Astro.props.name}>
  <Item {...Astro.props} />
</Layout>
